// import { PrismaClient } from './prisma-client'

// const globalForPrismaSupabase = global as unknown as { prismaSupabase: PrismaClient }

// export const prismaSupabase = globalForPrismaSupabase.prismaSupabase || new PrismaClient()

// if (process.env.NODE_ENV !== 'production') globalForPrismaSupabase.prismaSupabase = prismaSupabase

// export default prismaSupabase